// Token of the last public (no-login) treino link this browser viewed —
// used so that installing the PWA from a student's link, then reopening
// it later, lands back on that treino instead of the trainer login screen.
export const LAST_PUBLIC_TOKEN_KEY = 'lastPublicToken';
